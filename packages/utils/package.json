{
  "name": "@noz/utils",
  "version": "0.0.0",
  "license": "MIT",
  "description": "puede contener funciones y módulos que son específicos para la extracción de datos en sí. Por ejemplo, funciones que hacen solicitudes web y extraen datos de HTML o JSON, o módulos que manejan la autenticación y el control de sesiones en sitios web.",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "author": {
    "name": "Niels Olivera Zurita",
    "email": "nielsoz.dev@gmail.com"
  },
  "scripts": {
    "build": "bun run clean && tsc -p tsconfig.json",
    "clean": "bun rimraf tsconfig.tsbuildinfo && rimraf dist",
    "lint": "eslint . --fix"
  },
  "dependencies": {
    "chalk": "5.3.0"
  },
  "devDependencies": {
    "@noz/eslint-config": "workspace:*",
    "@noz/tsconfig": "workspace:*",
    "@types/node": "20.14.2",
    "rimraf": "6.0.0",
    "typescript": "5.4.5"
  }
}
